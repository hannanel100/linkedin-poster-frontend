import React from "react";
import Form from "../components/Form";

const Post = () => {
  return (
    <>
      <Form postId={undefined} setPostId={undefined} />
    </>
  );
};

export default Post;
